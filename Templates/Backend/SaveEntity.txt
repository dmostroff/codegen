<?php


namespace Domain\{$className}\Actions;

use Admin\{$className}\Queries\{$className}SaveQuery;
use Domain\{$className}\DTO\{$className}DTO;
use Domain\{$className}\Transformers\{$className}Transformer;

class Save{$className}
{
    private ${$entityName}Dto;

    function __construct ({$className}DTO ${$entityName}Dto)
    {
        $this->{$entityName}Dto = ${$entityName}Dto;
    }

    public function handle ()
    {
        ${$entityName} = {$className}Transformer::transformDTOToEntity($this->{$entityName}Dto);
        {$className}SaveQuery::execute(${$entityName});

        return ${$entityName};
    }

}
